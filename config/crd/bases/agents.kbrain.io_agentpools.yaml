---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: agentpools.agents.kbrain.io
spec:
  group: agents.kbrain.io
  names:
    kind: AgentPool
    listKind: AgentPoolList
    plural: agentpools
    singular: agentpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.activeAgents
      name: Active
      type: integer
    - jsonPath: .status.queuedTasks
      name: Queued
      type: integer
    - jsonPath: .status.completedTasks
      name: Completed
      type: integer
    - jsonPath: .spec.maxConcurrency
      name: Max
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentPool is the Schema for the agentpools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentPoolSpec defines the desired state of AgentPool.
            properties:
              defaultAPIKeySecretRef:
                description: DefaultAPIKeySecretRef is the default API key secret
                  for tasks in this pool.
                properties:
                  key:
                    description: Key within the Secret data. Defaults to the first
                      key if empty.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              defaultAgentType:
                description: DefaultAgentType for tasks in this pool.
                enum:
                - claude-code
                - aider
                - codex
                type: string
              defaultGitCredentialsSecretRef:
                description: DefaultGitCredentialsSecretRef is the default git credentials
                  secret for tasks in this pool.
                properties:
                  key:
                    description: Key within the Secret data. Defaults to the first
                      key if empty.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              defaultModelName:
                description: DefaultModelName for tasks in this pool.
                type: string
              defaultModelProvider:
                description: DefaultModelProvider for tasks in this pool.
                enum:
                - ollama
                - cloud
                type: string
              git:
                description: Git holds the default repository configuration for this
                  pool.
                properties:
                  baseBranch:
                    description: BaseBranch is the source branch to base work on.
                    type: string
                  platform:
                    default: github
                    description: Platform is the git hosting platform for MR/PR creation.
                    enum:
                    - github
                    - gitlab
                    - bitbucket
                    type: string
                  repoURL:
                    description: RepoURL is the git repository URL (HTTPS or SSH).
                    type: string
                  targetBranch:
                    description: TargetBranch is the branch the MR/PR targets. Defaults
                      to BaseBranch.
                    type: string
                  workBranch:
                    description: WorkBranch is the branch name the agent will create.
                      Auto-generated if empty.
                    type: string
                required:
                - baseBranch
                - repoURL
                type: object
              maxConcurrency:
                default: 3
                description: MaxConcurrency is the maximum number of concurrent agent
                  tasks.
                format: int32
                minimum: 1
                type: integer
              queueStrategy:
                default: fifo
                description: 'QueueStrategy determines task ordering: fifo or priority.'
                enum:
                - fifo
                - priority
                type: string
              selector:
                description: Selector matches AgentTasks belonging to this pool.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - git
            - maxConcurrency
            type: object
          status:
            description: AgentPoolStatus defines the observed state of AgentPool.
            properties:
              activeAgents:
                description: ActiveAgents is the number of currently running agents.
                format: int32
                type: integer
              activeTaskNames:
                description: ActiveTaskNames lists the names of currently active tasks.
                items:
                  type: string
                type: array
              completedTasks:
                description: CompletedTasks is the total number of completed tasks.
                format: int32
                type: integer
              conditions:
                description: Conditions provide detailed status information.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedTasks:
                description: FailedTasks is the total number of failed tasks.
                format: int32
                type: integer
              queuedTasks:
                description: QueuedTasks is the number of tasks waiting for a slot.
                format: int32
                type: integer
            required:
            - activeAgents
            - completedTasks
            - failedTasks
            - queuedTasks
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
